# Rules index

Binding guardrails. Each rule is its own `.md` file and must also be listed under
"Rules" in the root `CLAUDE.md`. None defined yet — add them as constraints emerge
(test discipline, migration safety, never commit `.env`, etc.).
